const HomeRoot = '/';
const ProfileRoot = '/profile';
const Search = '/search';
const Book = '/book';

const AuthRoot = '/auth';
const Login = `${AuthRoot}/login`;
const Register = `${AuthRoot}/register`;

export default {
    HomeRoot,
    ProfileRoot,
    Search,
    Book,
    AuthRoot,
    Login,
    Register,
};
