const HomeRoot = '/';

const ProfileRoot = '/profile';

const Search = '/search';

const AuthRoot = '/auth';
const Login = `${AuthRoot}/login`
const Register = `${AuthRoot}/register`

export default {
    HomeRoot,
    ProfileRoot,
    Search,
    AuthRoot,
    Login,
    Register
};
