import React from 'react';
import { Breadcrumbs } from '@gravity-ui/uikit';
import URLs from '~/constants/URLs';

export const HotelBreadCrumbs: React.FC = () => {
    // return (
    //     <Breadcrumbs
    //         firstDisplayedItemsCount={0}
    //         items={[].map((val) => ({
    //             text: 'Москва',
    //             href: `${URLs.Search}?regionId=1`,
    //         }))}
    //     />
    // );
    return null;
};
